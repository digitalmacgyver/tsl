Scene headings := 

      Starts with { INTERIOR, EXTERIOR, INT., EXT. } and has no
      lowercase characters.

Action line := Starts with non-whitespace, is not a scene heading.

Dialog header := Starts with whitespace followed by non-lowercase characters.

Dialog := Starts with whitespace followed by non-whitespace.

